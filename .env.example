# Optional: Set a custom output directory for exported line sheets
# OUTPUT_DIR=./output

# Optional: Set request timeout for website scraping (seconds)
# REQUEST_TIMEOUT=30

# Optional: Set max concurrent image downloads
# MAX_IMAGE_DOWNLOADS=10
